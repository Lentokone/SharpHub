@using SharpHub.Models
@using SharpHub.Models.Services

<div class="container mt-4">

@* @if (ModalState)
    {
        <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);" @onclick="OnBackdropClick">
            <div class="modal-dialog modal-dialog-centered" @onclick:stopPropagation="true">
                <div class="modal-content shadow-lg">

                    <div class="modal-header">
                        <h5 class="modal-title">Delete Repository</h5>
                        <button type="button" class="btn-close" aria-label="Close" @onclick="HideModal"></button>
                    </div>

                    <div class="modal-body">
                        <EditForm Model="@newRepo" OnValidSubmit="HandleValidSubmit">
                            <DataAnnotationsValidator />
                            <div class="mb-3">
                                <label for="RepositoryName" class="form-label">Repository Name</label>
                                <InputText id="RepositoryName" class="form-control" @bind-Value="newRepo.RepositoryName" />
                                <ValidationMessage For="@(() => newRepo.RepositoryName)" class="text-danger small" />
                            </div>
                            <div class="d-flex justify-content-end mt-3">
                                <button class="btn btn-primary">Yes</button>
                                <button class="btn btn-outline-secondary ms-2" @onclick="HideModal">Cancel</button>
                            </div>
                        </EditForm>
                    </div>
                </div>
            </div>
        </div>
    } *@

    <!-- Repo Header -->
    <div class="mb-4">
        <h1 class="fw-bold">@Model.RepositoryName</h1>
        <p class="text-muted">Repository description goes here…</p>
    </div>

    <!-- Clone Section -->
    <div class="card mb-4">
        <div class="card-body d-flex justify-content-between align-items-center">
            <span class="fw-semibold">Clone this repository</span>
        </div>
    </div>
    <div class="card mb-4">
        <div class="card-body d-flex justify-content-between align-items-center">
            <span class="fw-semibold">@testi</span>
        </div>
    </div>

    <div class="row">

        <!-- Tree Sidebar -->
        <div class="col-md-4 mb-4">
            <div class="card h-100">
                <div class="card-header fw-semibold">
                    Repository Tree
                </div>
                <div class="card-body">
                    <p class="text-muted">[ File tree placeholder ]</p>
                </div>
            </div>
        </div>

        <!-- README Renderer -->
        <div class="col-md-8 mb-4">
            <div class="card h-100">
                <div class="card-header fw-semibold">
                    README.md
                </div>
                <div class="card-body">
                    <p class="text-muted">[ README content placeholder ]</p>
                    <Markdown style="max-height: 437px;overflow-y:auto;">@markdown</Markdown>
                </div>
            </div>
        </div>

    </div>
</div>
<h1>@testi</h1>

@code {
    [Parameter]
    public required RepositoryDetailsViewModel Model { get; set; }

    private string markdown = "# Heading 1\n## Heading 2\n\n![BlazorBootstrap](https://demos.blazorbootstrap.com/images/logo/logo-color.svg =50)";
    string testi = "Testimuuttuja";

    private bool ModalState;
    // TOIMIII!!!!!!!!!!!!!
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            testi = "Tästä sitten se oikea load juttu? Mikä load juttu? Siis repository data ja muut.";
            StateHasChanged();
        }
    }

    private void ShowModal()
    {
        ModalState = true;
    }

    private void CloseModal()
    {
        ModalState = false;
    }

    private async Task HandleValidSubmit()
    {
        // Hmm.
        // Save käy.
        // Kunhan se nappaa oikean Repository modelin ja vaihtaa sen repositorystate false
        await Task.Run(() =>
        {
            @* MongoManipulator.Save("apina"); *@
        });

        StateHasChanged();
        CloseModal();
    }
}