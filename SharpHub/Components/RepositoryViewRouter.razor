@using SharpHub.Models

@if (Model.IsRepoDetails)
{
    <RepositoryDetails Model="Model.RepositoryDetailsViewModel" />
}
else
{
    <RepositoryList Model="Model.RepositoryListViewModel"/>
}

@code {
    [Parameter]
    public required RepositoryManagerViewModel Model { get; set; }
}